import math

def get_k_nearest(instance, k, data):
  distances_array = []
  for example in data:




def distance(instance1, instance2):
  instance1_values = list(instance1.values())
  instance2_values = list(instance2.values())
  for i in range (0, len(instance1_values)):
      if instance1_values[i]
  math.sqrt()